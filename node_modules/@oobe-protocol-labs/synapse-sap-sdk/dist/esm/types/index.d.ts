/**
 * @module types
 * @description Strongly-typed domain models for SAP v2.
 *
 * Every type mirrors the onchain Rust struct / enum exactly.
 * Types are organized into four layers:
 *
 * - **enums** — Anchor-style enum variant objects (`TokenType`, `ToolCategory`, …)
 * - **common** — Shared helper structs (`Capability`, `PricingTier`, `Settlement`, …)
 * - **accounts** — Deserialized on-chain PDA account data (`AgentAccountData`, …)
 * - **instructions** — Instruction argument DTOs + permission/schema helpers
 *
 * @category Types
 * @since v0.1.0
 *
 * @example
 * ```ts
 * import type { AgentAccountData, RegisterAgentArgs } from "@synapse-sap/sdk/types";
 * import { TokenType, SettlementMode } from "@synapse-sap/sdk/types";
 * ```
 */
export { TokenType, PluginType, SettlementMode, ToolHttpMethod, ToolCategory, SettlementSecurity, DisputeOutcome, BillingInterval, DisputeType, ResolutionLayer, } from "./enums";
export type { TokenTypeKind, PluginTypeKind, SettlementModeKind, ToolHttpMethodKind, ToolCategoryKind, SettlementSecurityKind, DisputeOutcomeKind, BillingIntervalKind, DisputeTypeValue, ResolutionLayerKind, } from "./enums";
export type { Capability, VolumeCurveBreakpoint, PricingTier, PluginRef, Settlement, } from "./common";
export type { AgentAccountData, AgentStatsData, FeedbackAccountData, CapabilityIndexData, ProtocolIndexData, ToolCategoryIndexData, GlobalRegistryData, MemoryVaultData, SessionLedgerData, EpochPageData, VaultDelegateData, SessionCheckpointData, ToolDescriptorData, EscrowAccountData, AgentAttestationData, MemoryLedgerData, LedgerPageData, EscrowAccountV2Data, PendingSettlementData, DisputeRecordData, ReceiptBatchData, AgentStakeData, SubscriptionData, CounterShardData, IndexPageData, } from "./accounts";
export type { RegisterAgentArgs, UpdateAgentArgs, GiveFeedbackArgs, UpdateFeedbackArgs, PublishToolArgs, UpdateToolArgs, InscribeToolSchemaArgs, InscribeMemoryArgs, CompactInscribeArgs, CreateEscrowArgs, CreateAttestationArgs, DelegatePermissionBit, SchemaTypeValue, CompressionTypeValue, CreateEscrowV2Args, CreateSubscriptionArgs, } from "./instructions";
export { DelegatePermission, SchemaType, CompressionType, } from "./instructions";
export type { EndpointDescriptor, HealthCheckDescriptor, ToolManifestEntry, AgentManifest, EndpointValidationResult, } from "./endpoint";
//# sourceMappingURL=index.d.ts.map